<div class="container">
  <div class="container-fluid">
    <img src="<%= @place.picture %>" class="container-fluid" alt="<%= @place.name %>">
  </div>
  <div class="container-fluid">
    <h3 class="container-fluid"><%= @place.name %></h3>
  </div>
  <div class="container-fluid">
    <span class="container-fluid"><%= @place.description %></span>
  </div>
</div>