<%= form_for (@place), html: {multipart: true} do |f| %>
  <%= render 'shared/error_messages' %>
  <%= f.label :name %>
  <%= f.text_field :name, class: "form-control", placeholder: "Example: Costa Rica" %>
  <%= f.label :description %>
  <%= f.text_area :description, class: "form-control", placeholder: place_description %>
  <%= f.file_field :picture, accept: 'image/jpeg,image/png,image/jpg', class: "form-control" %>
  <%= f.submit "Save", class: "btn btn-primary save-place" %>
<% end %>