<ul class="nav justify-content-end">
  <% if user_signed_in? %>
    <li class="nav-item active">
      <a class="nav-link disabled"><%= current_user.email %> <span class="sr-only">(current)</span></a>
    </li>
    <li class="nav-item">
      <%= link_to "My places (#{current_user.places.count})", my_places_path, class: "nav-link" %>
    </li>
    <li class="nav-item">
      <%= link_to "Logout", destroy_user_session_path, method: :delete, data: {confirm: 'Are you sure?'}, class: "nav-link" %>
    </li>
  <% end %>
  <li class="nav-item">
    <button type="button" class="<%= press_button %> nav-link btn btn-primary" rel="background-places_form">Add
      place
    </button>
  </li>
</ul>