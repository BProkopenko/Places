<div id="background-places_form" class="container-fluid">
  <div id="pop-up-form" class="<%= form_class %> container col-md-3">
    <div class="close-form">Close[X]</div>
    <div>
      <% if user_signed_in? %>
        <%= render 'places/place_form' %>
      <% else %>
        <%= render 'shared/sign_up_form' %>
      <% end %>
    </div>
  </div>
</div>
<script type="text/javascript">
    $('#place_picture').bind('change', function () {
        size_in_megabytes = this.files[0].size / 1024 / 1024;
        if (size_in_megabytes > 5) {
            alert('Maximum file size is 5MB. Please choose a smaller file.');
        }
    });
</script>
